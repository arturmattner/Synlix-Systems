<section id="container-main">

  <section class="section">
    <h2>Products</h2>
  </section>
  <section class="section">
    <h2><%= link_to 'CPU', '#', :class => "cpu-drop" %></h2>
    <section class="section cpu-section">
      <%= render @cpus %>
      <%= paginate @cpus, :remote => true %>
    </section>
    
  </section>
  
  <section class="section">
    <h2><%= link_to 'RAM', '#', :class => "ram-drop" %></h2>
    <section class="section ram-section">
      <%= render @rams %>
      <%= paginate @rams, :remote => true %>
    </section>
  </section>

  <section class="section">
    <h2><%= link_to'Graphics', '#', :class => "gfx-drop" %></h2>
    <section class="section gfx-section">
      <%= render @gfxes %>
      <%= paginate @gfxes, :remote => true %>
    </section>
  </section>
</section>